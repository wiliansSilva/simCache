import java.util.Random;

public class ConjuntoRandom {
    Random random = new Random();
    int indice;
    public ConjuntoRandom(int indice){
        this.indice = indice;
    }

    String acessarBloco(int indice, Conjuntos[] conjuntos){
        for(int i = 0; i < conjuntos.length; i++){
            if(conjuntos[i] != null && conjuntos[i]){

            }
        }
    }

}
